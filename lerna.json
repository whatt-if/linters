{
  "version": "independent",
  "npmClient": "yarn",
  "packages": ["packages/*"],
  "command": {
    "publish": {
      "conventionalCommits": true,
      "ignoreChanges": ["**/*.md", "@whatt-if/linters"],
      "message": "chore(release): lerna publish"
    },
    "version": {
      "conventionalCommits": true
    }
  }
}
